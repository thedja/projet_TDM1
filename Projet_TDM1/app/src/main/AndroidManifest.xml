<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="devmobile.projet_tdm1">

<application
    android:allowBackup="true"
    android:icon="@drawable/logo"
    android:label="@string/app_name"
    android:theme="@style/AppTheme"
    android:name=".MyApplication"
    >
    <activity
        android:name=".ProgramDetailActivity"
        android:label="@string/app_name">
        <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".ChannelModeActivity"/>
    </activity>
    <activity

        android:name=".ChannelModeActivity"
        android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name=".ThematiqueModeActivity"
        android:label="@string/app_name">
    </activity>
    <activity
        android:name=".TrancheHoraireModeActivity"
        android:label="@string/app_name">
    </activity>
    <activity
        android:name=".FavorisModeActivity"
        android:label="@string/app_name">
    </activity>
</application>

</manifest>

